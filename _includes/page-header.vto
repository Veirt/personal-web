<header class="page__header mb-10 relative overflow-hidden pb-6 border-b border-border">
    <h1 class="text-2xl sm:text-3xl font-semibold leading-tight tracking-tight text-text m-0">{{ title }}</h1>
    {{ if subtitle }}
        <p class="font-mono text-sm text-accent/60 mt-2 m-0 leading-relaxed">{{ subtitle }}</p>
    {{ /if }}
</header>
