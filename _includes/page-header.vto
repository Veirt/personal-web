<header class="page__header text-center mb-12 relative overflow-hidden">
    <h1 class="text-4xl font-bold mb-2 text-primary leading-tight">{{ title }}</h1>
    {{ if subtitle }}
        <p class="text-lg text-accent m-0 font-normal leading-relaxed">{{ subtitle }}</p>
    {{ /if }}
</header>
